if(input.Due_Date > today)
{
	thisapp.blueprint.changeStage("Debit_Note","Debit_Note_Blueprint","Sent",input.ID);
	input.Status = "Sent";
}
else
{
	thisapp.blueprint.changeStage("Debit_Note","Debit_Note_Blueprint","Overdue",input.ID);
	input.Status = "Overdue";
}
