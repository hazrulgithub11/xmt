transition owner
            				(
                				permissions = {"Administrator", "Finance Manager"}
             				)
            				confirmation before execution
            				(
                				message = "Are you sure you want to 'Approve' for this Deposit ${Invoice_No}?"
                				confirm button text = "Yes"
                				cancel button text = "No"
            				)
