Bulk_Approve_Invoices as "Bulk Approve Invoices"
{
	type =  functions
	form = Invoice
	execution type = for each record    
	on start
	{
		actions 
		{
		on click
		(
			thisapp.blueprint.executeTransition("Invoice","Invoice_Blueprint","Approve",input.ID);
		)
		}
	}
}

