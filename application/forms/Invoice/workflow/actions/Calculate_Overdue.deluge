Calculate_Overdue as "Calculate Overdue"
{
	type =  functions
	form = Invoice
	execution type = for each record    
	on start
	{
		actions 
		{
			thisapp.invoice.recalculate_overdue(Customer_Code,ID);
		}
	}
}

