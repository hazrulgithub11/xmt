Validation_Based_On_Statu as "Validation Based On Status"
{
	type =  form
	form = Invoice

	record event = on edit

	on validate
	{
		actions  (Blueprint.Current_Stage == "Rejected")
		{
			custom deluge script
			(
					if(input.Confirm_resubmission == false)
				{
					alert(Confirm_resubmission,"Please check to confirm resubmission");
					cancel submit;
				}
			)
		}
		actions  (Blueprint.Current_Stage == "Pending Approval")
		{
			custom deluge script
			(
					if(input.Rejection_Reason == "")
				{
					alert(Rejection_Reason,"This field is mandatory. Enter a value.");
					cancel submit;
				}
			)
		}
	}

}

