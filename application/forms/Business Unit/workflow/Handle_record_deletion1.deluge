Handle_record_deletion1 as "Handle record deletion"
{
	type =  form
	form = Business_Unit

	record event = on delete

	on validate
	{
		actions 
		{
			custom deluge script
			(
					alert "Business Unit cannot be deleted.";
				cancel delete;
			)
		}
	}

}

