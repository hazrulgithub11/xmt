Validate_Record_Deletion6 as "Validate Record Deletion"
{
	type =  form
	form = Tax

	record event = on delete

	on validate
	{
		actions 
		{
			custom deluge script
			(
					alert "This tax can’t be deleted.";
				cancel delete;
			)
		}
	}

}

